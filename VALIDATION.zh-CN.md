# 快照验收记录 · [English](VALIDATION.md)

更新日期为 2026-09-14。本文分开记录实现测试、文档检查、纸面论证与 Lean 内核验证，不把其中一项冒充另一项。

## 交付文档

- RPD、LRD、Ω-LRD3、ARD、IPD、ARD2、SPD：七套定义，各有中英文 Markdown 与 PDF，共 **28 份定义文件**。
- Y、RPD、LRD、Ω-LRD3：原有合写证明；ARD、IPD 和 ARD2：各自的独立完整证明。四篇均有中英文 Markdown 与 PDF，共 **16 份证明文稿文件**。IPD 的直接 KP 树秩引理完整收入附录 A，不依赖仓库外研究文件。
- IPD 另有中英文定义对应审计。
- SPD 新增双语纸面论证稿及 PDF，共四份，使证明文稿总数为 **20 份**。SPD 尚无 Lean 证明或验证收据；原七个 Lean 项目及联合定理不变。
- 总 README、Lean 说明、来源说明与本文也均有中英文 Markdown。每份英文发布文档标题链接到对应中文版；另存的历史研究归档含明确列名的单语原稿。
- 发布文件清单不包含其他 Ω-LRD 实现、历史证明尝试、私人原始 PDF、已安装依赖及预编译证明缓存。

原有合写论文的中英文版均有 25 个展示公式、15 个带编号公式，顺序一致。新增 ARD 论文英文有 34 个展示公式、中文有 33 个；唯一差异是可数有界性公式在英文独立展示、中文行内展示。其 15 个编号公式一致，ARD 两份定义的 6 个展示公式也一致。数学主体与假设已独立核对。定义稿与证明交叉检查了三元组次序、控制优先级、生成包上限、种子、标准域及顶端约定。ARD 中英文定义中的程序示例均已使用发布版 Python 模块执行通过。

IPD 两种语言的证明各有 31 个展示公式，12 个公式编号（含附录）完全一致。英文定义有 10 个展示公式、中文 9 个：差异是原头换位候选在英文展示、中文以行内代码元组呈现；其余差异是行内排版，不改变定义或假设。发布版 Python 示例已执行通过。

ARD2 两份证明各有 25 个展示公式，相同的 20 个编号公式；除公理体系名称的翻译外，去除空白后所有展示公式主体一致。两份定义各有六个展示公式，其中一个含翻译文字。四段文本示例与 Python API 示例一致，Python 示例实际执行通过。

## 展开器测试

在本包根目录运行：

```sh
python -B tests/test_python.py
python -B tests/test_ard.py
python -B tests/test_ipd.py
python -B tests/test_ard2.py
python -B tests/test_spd.py
node --max-old-space-size=256 tests/ard2_ner.cjs
node --max-old-space-size=256 tests/ard2_display.cjs
node --max-old-space-size=256 tests/ipd_display.cjs
node --max-old-space-size=256 tests/adjacency_views.cjs
```

七个测试方法均通过。有界样本包含：

| 检查 | 次数 |
| --- | --- |
| Python 展开及前缀检查 | 608 |
| 跨语言输入样例 | 160 |
| Python/NER 展开对照 | 640 |
| Python/NER 大小比较对照 | 400 |
| Ω-LRD3 与独立元组参考的展开对照 | 152 |

测试设 30 秒总体检查点时限，队列与状态大小有界。Node 对照进程设 15 秒超时与 256 MiB 老生代堆上限。不使用一直迭代到零的反例搜索。纯 Python 测试不强制安装 Node.js；缺少 Node 时跳过 JS 对照。本次记录对应 Node 已安装的实际运行。

独立 ARD 测试通过：120 个图及 480 次独立全根规则展开对照、7 个种子、8 个非法图、非法下标、9 个 ARD/RPD 边界样例，以及公共后裔中的 48 个图、192 次展开。跨语言检查包含 216 个输入、864 次展开、455 次比较，以及与有界朴素局部计数参考核对的 17 项精确计数。此测试设 40 秒检查点时限，Node 进程设 20 秒超时及 256 MiB 老生代堆上限。两个 Python 测试命令分别耗时约 0.6 秒和 2.0 秒。

ARD 弧线绘制测试另通过：240 个随机样例、480 次展开、480 次比较、241 次计数、22 幅图及其中 525 个关系/根框、2,395 项轨道检查、24,601 项标签对检查、8,710 项已画线段检查、17,036 项框线段检查、6 对翻转及 8 项保护检查。运行约 1.1 秒，老生代堆上限 192 MiB，报告的 RSS 峰值约 126 MiB。Node 的这些限制不是进程总内存硬上限。这是几何与 API 检查，并非真实浏览器截图测试。

原有三份 Python 命令行示例也通过。LRD、Ω-LRD3 的 NER 文件保持逐字节不变；更新后的 RPD、ARD 完整嵌入提交 `542d47a` 中此前发布的原脚本，在其外追加注册信息及两种邻接表显示。所有交付文件 SHA-256 见 [SOURCES.zh-CN.md](SOURCES.zh-CN.md)。Ω-LRD3 元组参考另有有界自检通过：7 个种子、37 个状态、148 项下降检查及 111 项前缀检查。

可移植邻接表测试通过：168 个图及文字往返、672 次未变的展开、160 次比较、498 项文字前缀检查、168 张完整图及其中 942 个关系格、420 次旧显示对照、20 项输入/资源保护检查。测试用换行归一化源码哈希固定嵌入的旧核心，并核对两份新增显示辅助代码一致。几何检查覆盖完整淡色对角编号格、阶梯形上三角轮廓、取消外侧坐标标记、表格上方复用原精确计数，以及计数超限时保留完整关系表。运行约 1.3 秒，老生代堆上限 256 MiB，报告的 RSS 约 87 MiB。亮色、暗色、空图及多位数的完整预览已离线渲染并目视检查；不是实际浏览器端到端测试。本次显示更新未改数学定义和证明源码。

新增 IPD 测试通过：247 个图（117 个标为标准式）、988 次 Python/JS 展开、741 次完整前缀、500 次大小比较、231 组精确局部计数、453 次计数减一、1,292 项较大闭式计数、247 次完整树显示和 22 项保护检查，另有 16 级 Python 种子检查。有界参考明确跳过了 8 个昂贵展开和 16 个计数样例，跳过不计成功。本次总耗时 5.56 秒，Node 报告 RSS 130 MiB，老生代堆上限 512 MiB；包装器给 Python 30 秒、Node 55 秒，并清理自身进程树。独立显示回归通过 12 个样例、76 个节点、9 个嵌套头框、51 条子连边和 2 项保护。不依赖 NER 源码，不声称真实浏览器测试。原 Python、ARD、邻接表和弧线测试也全部重新通过，未改旧定义。

ARD2 独立原子参考通过 37,044 个图、111,301 次展开，包含宽度不超过三的全部规范合法图、种子和有界随机样例。Python 用时约 11.1 秒，峰值 RSS 25.3 MiB。Node 通过 996 次独立原子步、676 次 Python→JavaScript 展开向量、169 次比较及七组计数，约一秒、RSS 83 MiB。显示测试通过八个样例、153 个完整弧线组、34 个表格项、七次文字往返及五项保护，包括计数耗尽时仍显示完整几何。Node 堆为 256 MiB；测试检查 512 MiB RSS 及明确时间／工作量上限。真实 JS 输出的七张完整亮／暗 SVG 预览已渲染并目视检查，这是离线呈现测试，不是 NER 实时会话。旧六条测试命令也独立重新通过，全部此前跟踪的记号和 Lean 数学源码保持不变。

SPD 的统一测试入口独立可运行，不导入私人研究文件。独立元组参考通过 65 个标准态／260 次展开、45 个 raw 图／180 次展开、100 对比较及 162 次局部计数减一，另有九项非法输入、四项资源保护和一条 1,200 列的迭代链。混合父列检查通过 160 个 raw 图／640 次展开／441 次计数减一。解码器精确还原全部 65 个标准态，完整分类宽不超过三的 86 个计数词（43 合法／43 非法），并检验 raw 计数碰撞不能误认标准性。

SPD NER 测试通过 65 个标准态／260 次展开。Python→JavaScript 向量在 44 个图／176 次展开／100 对比较上一致，计数解码的 106 项输入一致（63 接受／43 拒绝）。统一运行约 18 秒；Python 峰值 RSS 小于 35 MiB，监控界为 256 MiB；Node 老生代堆限制 256 MiB，报告 RSS 小于 130 MiB。包装器有 54 秒总体检查点时限，各子进程最多 50 秒，超时终止并等待自身直接子进程；这些子进程不再派生进程。分配、队列和迭代另有明确上限。定义中的 Python 示例也独立运行通过。这里是注册接口桩测试，不是真实 NER 网页点击。

此次 SPD 入库还重新通过原 Python、ARD、IPD、ARD2、显示、邻接表及 Lean 验证器回归测试。原六套记号实现和全部 Lean 源码／收据保持不变，不声称新增一次 Lean 内核编译。

上述均为有限测试，不证明所有输入等价，不认证任意可解析图的标准性，也不替代良序证明。本次整理未重新进行完整的真实浏览器集成测试。

## PDF 检查

发布包包含 **24 份 PDF／158 页**，使用 ReportLab、MathJax 公式及嵌入字体。此前二十份 PDF／124 页逐字节不变，保留已有目视检查记录。SPD 新增英文定义 8 页、中文定义 6 页、英文证明 11 页、中文证明 9 页。

新增 **34 页 SPD 文档**全部经 Poppler 渲染，通过文字／图片边界检查，零越界，并逐页在联系表中目视检查；英文定义第 4 页、英文证明第 8–9 页、中文证明第 7 页另以整页分辨率复核。中文定义略收紧排版，消除了原来只有几行的第七页；没有重新生成旧 PDF。

SPD 使用自己的标题及 2026-09-14 页脚。双语定义各有 18 个展示公式，双语证明各有 37 个展示公式及相同的 20 个公式标签；数学主体与假设已交叉核对。修正后的严格守卫和端点公式均正常显示，没有缺字或裁切。

[tools/pdf-build-report.json](tools/pdf-build-report.json) 记录页数与哈希；[QA 收据](tools/pdf-qa-report.json) 区分此次新增检查与此前 124 页的保留检查记录。命令见[主 README](README.zh-CN.md)，中间图片与公式缓存不进入发布清单。

## Lean 源码验证

证明源码并集仍为 **310 个随附模块＋12 个固定外部 BMS 模块，共 322 个**。拆分时 306 个文件移动到 shared 与七个记号的私有项目，四个联合模块留在原处。逐一核对了全部 310 个文件的原始字节，数学源码与模块名均未改变。

各项目具有独立的源码清单、Lake 配置、工具链、锁文件、构建输出和验证收据。精确闭包分别为 shared 35、Y 189、RPD 36、LRD 41、Ω-LRD3 44、ARD 50、IPD 55、ARD2 51；这些数字包含重复使用的共享依赖，不是互不相交的模块数。只有 Y 依赖外部 BMS。

`python lean/ARD2/build.py --check-only` 只核对 ARD2 的精确闭包、源码哈希与本项目 Lake 枚举，不需要加载其他记号。根目录 `python lean/build.py --check-only` 仅用于可选的全库检查。

拆分前七系统的 **322 模块／866 项公理报告**全新串行重建确已完成，耗时 1638.176 秒；[原样归档收据](lean/verification/SevenNotation-Monolithic-VERIFICATION.json)保留该次记录，不将其冒充新布局构建。

`python -B tests/test_lean_verifier.py` 的九组回归测试通过，覆盖九个项目的精确闭包、五类导入产物的指纹、逐模块清理、外部缓存遮蔽拒绝、公理日志名称与计数、陈旧／不完整收据、项目锁、无关项目元数据不改变指纹，以及当前导入语法和嵌套注释解析。测试仅使用临时数据，不把假产物交给 Lean；真实内核检查由下面的源码编译完成。

新布局的各项目已实际通过，收据分开保存：

| 项目收据 | 闭包模块 | 公理报告 | 本次新编译 | 明确认证后复用 |
| --- | --- | --- | --- | --- |
| [shared](lean/shared/VERIFICATION.json) | 35 | 93 | 35 | 0 |
| [Y](lean/Y/VERIFICATION.json) | 189 | 422 | 175 | 14 |
| [RPD](lean/RPD/VERIFICATION.json) | 36 | 91 | 4 | 32 |
| [LRD](lean/LRD/VERIFICATION.json) | 41 | 110 | 10 | 31 |
| [Ω-LRD3](lean/Omega-LRD3/VERIFICATION.json) | 44 | 119 | 13 | 31 |
| [ARD](lean/ARD/VERIFICATION.json) | 50 | 156 | 20 | 30 |
| [IPD](lean/IPD/VERIFICATION.json) | 55 | 166 | 40 | 15 |
| [ARD2](lean/ARD2/VERIFICATION.json) | 51 | 160 | 21 | 30 |
| [可选联合验收](lean/VERIFICATION.json) | 322 | 866 | 4 | 318 |

共享基础先全新编译，叶项目只复用本次核验的共享产物，联合工程只复用这些已核验的叶／共享产物并新编译四个联合模块。[汇总核对](lean/verification/IndependentProjects-VERIFICATION.json)逐模块比较指纹、完整导入产物哈希及公理报告，并确认首轮迁移验收中 **322 个不同模块恰好各从源码新编译一次，866 项不同公理报告全部通过**。各行闭包包含重叠，不能直接相加当作不同模块的数量。

真实 Lake 的九套配置共通过 18 项加载、唯一归属、默认枚举及导入查找检查；[配置与迁移记录](lean/verification/ProjectLayout-VERIFICATION.json)明确这不是一次完成的 `lake build`。在临时副本加入故意损坏的新项目和无关 Y 坏源码后，ARD2 的两项检查仍通过；直接加载坏项目则失败，构成负对照。

最后的只读一致性检查重新计算了实际编译器、隐式系统库及全部外部导入产物的哈希，并核对九个项目的公开／本地收据、完整导入产物组和日志，全部通过。这项检查不重新运行 Lean 内核。在额外的 `--resume` 测试中，RPD、ARD2 各遇到一次 Windows 瞬时文件替换拒绝；失败检查点正确标为不完整，原公开成功收据保持不变，两者重跑后均通过。首轮编译统计与这些后续重试分开记录。

源码验证器只接受报告中的 `propext`、`Classical.choice`、`Quot.sound` 或其子集。编译采用既有 Lean 与固定 Mathlib／辅助依赖产物，但本地证明从已核对的源码生成；不声称完成网络全新初始化或 Mathlib 源码重建。有界构建与缓存输入规则见 [Lean 说明](lean/README.zh-CN.md)，生成二进制及本地缓存不进入发布目录。

本次文档链接迁移仅改变相对源码 URL，不改变公式或可见标签；PDF 渲染器对相对链接仅输出标签，因此 20 份 PDF 保持不变。ARD2 JS／Python 内的三处旧 Lean 地址另作说明性修正，展开规则不变；其他五个记号的实现保持原样。

## 保留的明确边界

四篇论文均在保留完整集合归纳的 `KP_ω + 存在不可数序数` 内论证；普通 Lean 证明不是该体系中的对象语言推导。论文使用的有限几何接口链接到已核验的具体源码实现。ARD 的 Lean 还证明了与独立有限并展开规则及标准域的等价，以及压缩表示与全根表示的比较、控制项严格一致；这不等于逐行形式化验证全部 Python 或 JavaScript 运行时代码。

ARD2 的 Lean 实际构造双 SELF 关系与无条件初始供应，证明完整逐块拼接，并连接独立纸面有限规则及可达域。标准子类型不假设可及性或可表示性。纸面弱 KP 构造与普通 Lean 证明仍是不同层面的结论。

IPD 的 Lean 证明使用真实全位置数学算法；源语言对应和 JS 精确两位置剪枝由[纸面审计](proofs/paper/ipd-fidelity.zh-CN.md)给出，不是 JS 虚拟机形式化。IPD 标准域定义不假设可及性或可表示性；不新增与 wY/TPD 比较或共尾性主张。

良序结论针对文中指定的标准域及顶端约定，不是任意可解析图上的按列比较。Y 定理针对固定上游的继承祖先定义，它与原 Naruyoko JavaScript 全合法域等价仍不在本包声明范围内。本次打包也不建立最优公理强度、证明论序数比较或七个 Lean 系统之间及涉及 SPD 的序型大小不等式。

运行 `python tools/check_release.py` 可核对语言配对、相对入口链接、二十四份 PDF 及其源稿和构建哈希、NER 快照哈希、私人路径排除、Lean 源码依赖集合，以及公开验证记录与当前清单、逐模块依赖指纹、构建输入、最终定理日志的一致性。此一致性检查不会重新运行 Lean 内核。

SPD 入库最终检查通过 **73 份 Markdown：52 份双语发布文件及 21 份明确列名的单语归档**、24 份 PDF、七份固定 NER 脚本，以及原九个 Lean 验证范围／322 个不同模块。旧检查器误把这 21 份已跟踪、未改动的研究原稿也要求翻译；现在逐路径例外只豁免语言配对和互链，仍检查标题格式、公式、本地链接、私人路径及全部 Lean 条件，名单内文件缺失也会失败。新文件不会因所在目录而自动豁免。
