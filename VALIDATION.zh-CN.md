# 快照验收记录 · [English](VALIDATION.md)

整理日期为 2026-09-13。本文分开记录实现测试、文档检查、纸面论证与 Lean 内核验证，不把其中一项冒充另一项。

## 交付文档

- RPD、LRD、Ω-LRD3、ARD：四套定义，各有中英文 Markdown 与 PDF，共 **16 份定义文件**。
- Y、RPD、LRD、Ω-LRD3：原有合写证明；ARD：独立完整证明。两篇均有中英文 Markdown 与 PDF，共 **8 份证明文稿文件**。
- 总 README、Lean 说明、来源说明与本文也均有中英文 Markdown。每份英文标题链接到对应中文版。
- 发布文件清单不包含其他 Ω-LRD 实现、历史证明尝试、私人原始 PDF、已安装依赖及预编译证明缓存。

原有合写论文的中英文版均有 25 个展示公式、15 个带编号公式，顺序一致。新增 ARD 论文英文有 34 个展示公式、中文有 33 个；唯一差异是可数有界性公式在英文独立展示、中文行内展示。其 15 个编号公式一致，ARD 两份定义的 6 个展示公式也一致。数学主体与假设已独立核对。定义稿与证明交叉检查了三元组次序、控制优先级、生成包上限、种子、标准域及顶端约定。ARD 中英文定义中的程序示例均已使用发布版 Python 模块执行通过。

## 展开器测试

在本包根目录运行：

```sh
python -B tests/test_python.py
python -B tests/test_ard.py
```

七个测试方法均通过。有界样本包含：

| 检查 | 次数 |
| --- | --- |
| Python 展开及前缀检查 | 608 |
| 跨语言输入样例 | 160 |
| Python/NER 展开对照 | 640 |
| Python/NER 大小比较对照 | 400 |
| Ω-LRD3 与独立元组参考的展开对照 | 152 |

测试设 30 秒总体检查点时限，队列与状态大小有界。Node 对照进程设 15 秒超时与 256 MiB 老生代堆上限。不使用一直迭代到零的反例搜索。纯 Python 测试不强制安装 Node.js；缺少 Node 时跳过 JS 对照。本次记录对应 Node 已安装的实际运行。

独立 ARD 测试通过：120 个图及 480 次独立全根规则展开对照、7 个种子、8 个非法图、非法下标、9 个 ARD/RPD 边界样例，以及公共后裔中的 48 个图、192 次展开。跨语言检查包含 216 个输入、864 次展开、455 次比较，以及与有界朴素局部计数参考核对的 17 项精确计数。此测试设 40 秒检查点时限，Node 进程设 20 秒超时及 256 MiB 老生代堆上限。两个 Python 测试命令分别耗时约 0.6 秒和 2.0 秒。

ARD 弧线绘制测试另通过：240 个随机样例、480 次展开、480 次比较、241 次计数、22 幅图及其中 525 个关系/根框、2,395 项轨道检查、24,601 项标签对检查、8,710 项已画线段检查、17,036 项框线段检查、6 对翻转及 8 项保护检查。运行约 1.1 秒，老生代堆上限 192 MiB，报告的 RSS 峰值约 126 MiB。Node 的这些限制不是进程总内存硬上限。这是几何与 API 检查，并非真实浏览器截图测试。

原有三份 Python 命令行示例也通过，三份 NER 文件保持逐字节不变。发布的 ARD 弧线脚本仅相对选定研究快照改了两处证明状态文字，未修改规则、比较、绘图或保护阈值。所有交付文件 SHA-256 见 [SOURCES.zh-CN.md](SOURCES.zh-CN.md)。Ω-LRD3 元组参考另有有界自检通过：7 个种子、37 个状态、148 项下降检查及 111 项前缀检查。

上述均为有限测试，不证明所有输入等价，不认证任意可解析图的标准性，也不替代良序证明。本次整理未重新进行完整的真实浏览器集成测试。

## PDF 检查

发布包包含 **12 份 PDF / 66 页**，使用 ReportLab、MathJax 公式及嵌入字体。原有八份 PDF / 41 页保持不变。四份新增 ARD 文件中，两份定义各 3 页，英文证明 11 页，中文证明 8 页。新增 25 页均逐页通过联系表目视检查，并以整页分辨率检查了部分公式密集页与末页。原有页面保留之前的目视检查结果。全部 66 页重新经 Poppler 渲染，文本和图片的程序化边界检查均通过，无越界。

现有渲染器保留编号公式及中文行内公式断行处理。ARD 检查中发现并修正了两处排版问题：微调英文定义行距，消除只有一行的孤立末页；为中文证明的悬挂标点预留 4 点宽度。两份修改后的输出均重新渲染并目视检查，没有用空白占位替代公式。

[tools/pdf-build-report.json](tools/pdf-build-report.json) 记录每份文件的页数及 SHA-256；[QA 记录](tools/pdf-qa-report.json) 明确记录最终 66 页的具体检查范围。重新生成与 QA 命令见 [总 README](README.zh-CN.md)。中间 PNG 与公式缓存不属于发布文件清单，可用工具重新生成。

## Lean 源码验证

本包内置 247 个 Lean 源码模块，另记录 12 个固定版本的外部 BMS 模块。`python lean/build.py --check-only` 无需下载即可核对完整的 259 模块导入依赖集合及内置源码哈希。编译时提供 BMS 源码目录后，还会核对该外部依赖的实际字节哈希。

**全部 259 个证明源码模块均已串行重新编译通过，合计 647 项公理报告**。[公开验证记录](lean/VERIFICATION.json) 保存结果；[Lean 说明](lean/README.zh-CN.md) 链接最终定理及定义对应关系日志。ARD 新增 21 个证明模块，另有新的五系统验收模块汇总扩大后的依赖集合。先前的四系统记录及验收模块另行保留，不作为 ARD 的证明依据。

只重用已有的 Lean、固定 Mathlib 及其辅助依赖编译产物。发布构建从已核对的源码重新编译 Y、外部 BMS 和本地记号证明模块，不读取旧研究工程的证明缓存。这不是从网络全新初始化环境，也不是重新构建全部 Mathlib 依赖。Lake 配置另使用本地外部依赖替代路径进行离线检查。生成的构建缓存不属于发布文件清单。

报告只接受 `propext`、`Classical.choice`、`Quot.sound` 或其子集。资源限制及构建细节见 [lean/README.zh-CN.md](lean/README.zh-CN.md)。初始化外部依赖与重新编译本项目完整证明源码，是不同的验证范围。

## 保留的明确边界

两篇论文均在保留完整集合归纳的 `KP_ω + 存在不可数序数` 内论证；普通 Lean 证明不是该体系中的对象语言推导。论文使用的有限几何接口链接到已核验的具体源码实现。ARD 的 Lean 还证明了与独立有限并展开规则及标准域的等价，以及压缩表示与全根表示的比较、控制项严格一致；这不等于逐行形式化验证全部 Python 或 JavaScript 运行时代码。

良序结论针对文中指定的标准域及顶端约定，不是任意可解析图上的按列比较。Y 定理针对固定上游的继承祖先定义，它与原 Naruyoko JavaScript 全合法域等价仍不在本包声明范围内。本次打包也不建立最优公理强度、证明论序数比较或五者之间的序型大小不等式。

运行 `python tools/check_release.py` 可核对语言配对、相对入口链接、十二份 PDF 及其源稿和构建哈希、NER 快照哈希、私人路径排除、Lean 源码依赖集合，以及公开验证记录与当前清单、逐模块依赖指纹、构建输入、最终定理日志的一致性。此一致性检查不会重新运行 Lean 内核。
