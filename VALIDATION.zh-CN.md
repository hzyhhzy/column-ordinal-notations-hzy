# 快照验收记录 · [English](VALIDATION.md)

整理日期为 2026-09-13。本文分开记录实现测试、文档检查、纸面论证与 Lean 内核验证，不把其中一项冒充另一项。

## 交付文档

- RPD、LRD、Ω-LRD3：三套定义，各有中英文 Markdown 与 PDF，共 **12 份定义文件**。
- Y、RPD、LRD、Ω-LRD3：一篇完整合写证明，中英文 Markdown 与 PDF，共 **4 份证明文稿文件**。
- 总 README、Lean 说明、来源说明与本文也均有中英文 Markdown。每份英文标题链接到对应中文版。
- 发布文件清单不包含其他 Ω-LRD 实现、历史证明尝试、私人原始 PDF、已安装依赖及预编译证明缓存。

两篇证明的 25 个展示公式、15 个带编号公式顺序一致。除翻译的文字短语与空白外，展示公式已逐式核对。定义稿与合写论文交叉检查了三元组次序、控制优先级、生成包上限、种子、标准域及顶端约定。

## 展开器测试

在本包根目录运行：

```sh
python -B tests/test_python.py
```

七个测试方法均通过。有界样本包含：

| 检查 | 次数 |
| --- | --- |
| Python 展开及前缀检查 | 456 |
| 跨语言输入样例 | 121 |
| Python/NER 展开对照 | 484 |
| Python/NER 大小比较对照 | 300 |
| Ω-LRD3 与独立元组参考的展开对照 | 152 |

测试设 30 秒总体检查点时限，队列与状态大小有界。Node 对照进程设 15 秒超时与 256 MiB 堆上限。不使用一直迭代到零的反例搜索。纯 Python 测试不强制安装 Node.js；缺少 Node 时跳过 JS 对照。本次记录对应 Node 已安装的实际运行。

三份 Python 命令行示例也通过。NER 文件与选定原稿逐字节一致，SHA-256 见 [SOURCES.zh-CN.md](SOURCES.zh-CN.md)。Ω-LRD3 元组参考另有有界自检通过：7 个种子、37 个状态、148 项下降检查及 111 项前缀检查。

上述均为有限测试，不证明所有输入等价，不认证任意可解析图的标准性，也不替代良序证明。本次整理未重新进行完整的真实浏览器集成测试。

## PDF 检查

用 ReportLab 和 MathJax 公式生成八份 PDF，嵌入本机字体，共 **41 页**。所有页面都经 Poppler 渲染；检查正文与公式位置，包括编号公式、中文、表格、代码示例、页眉及页码。文本和图片的程序化边界检查未发现越界。最终中文论文为九页，略微收紧行距，避免留下几乎空白的第十页。

目视检查发现的两个渲染问题已在交付前修正：编号公式分开渲染公式主体与编号；中文断行安全处理行内公式图片。修正后重新渲染最新输出，没有用空白占位替代公式。

[tools/pdf-build-report.json](tools/pdf-build-report.json) 记录每份文件的页数及 SHA-256；[QA 记录](tools/pdf-qa-report.json) 记录最终 41 页的检查。重新生成与 QA 命令见 [总 README](README.zh-CN.md)。中间 PNG 与公式缓存已在检查后删除，可用工具重新生成，不属于发布文件。

## Lean 源码验证

本包内置 225 个 Lean 源码模块，另记录 12 个固定版本的外部 BMS 模块。`python lean/build.py --check-only` 无需下载即可核对完整的 237 模块导入依赖集合及内置源码哈希。编译时提供 BMS 源码目录后，还会核对该外部依赖的实际字节哈希。

**全部 237 个证明源码模块均已串行重新编译通过，合计 554 项公理报告**。[公开验证记录](lean/VERIFICATION.json) 保存结果；[Lean 说明](lean/README.zh-CN.md) 链接五份最终定理及公理日志。仅在发布副本中删除未使用的旧记号声明，以及最后的注释更新，均包含在已检查的源码快照内。

只重用已有的 Lean、固定 Mathlib 及其辅助依赖编译产物。Y、外部 BMS 和本地记号证明模块均从已核对的源码编译，不读取旧研究工程的证明缓存。这不是从网络全新初始化环境，也不是重新构建全部 Mathlib 依赖。Lake 配置另通过实际 Lake 的离线加载与类型检查，使用本地外部依赖替代路径。公开记录及最终日志导出后，生成的构建缓存已删除。

报告只接受 `propext`、`Classical.choice`、`Quot.sound` 或其子集。资源限制及构建细节见 [lean/README.zh-CN.md](lean/README.zh-CN.md)。初始化外部依赖与重新编译本项目完整证明源码，是不同的验证范围。

## 保留的明确边界

合写论文在保留完整集合归纳的 `KP_ω + 存在不可数序数` 内论证；普通 Lean 证明不是该体系中的对象语言推导。论文使用的有限几何接口链接到已核验的具体源码实现。

Y 定理针对固定上游的继承祖先定义。它与原 Naruyoko JavaScript 全合法域等价仍不在本包声明范围内。本次打包也不建立最优公理强度、证明论序数比较或四者之间的序型大小不等式。

运行 `python tools/check_release.py` 可核对语言配对、相对入口链接、八份 PDF 及其源稿、NER 快照哈希、私人路径排除和 Lean 源码依赖集合。
