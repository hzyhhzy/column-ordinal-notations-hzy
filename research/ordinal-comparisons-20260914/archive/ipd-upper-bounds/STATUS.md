# IPD 表达式上界研究

> 历史研究稿归档。当前结论与证据等级以[研究总览](../../README.zh-CN.md)为准；此文件不属于正式良序证明或已验证的比较定理。原文的时间、运行结果和未完成项保留为历史记录。脚本命令以原工作区为基准，实验代码未随本目录分发。

2026-09-14 后续约定：用户明确接受固定证明仓库的 Y 与 Naruyoko Y 在关注标准域上等价，并要求不再检查 Naruyoko。以后统一使用 Y；不要重开旧等价审计或将它反复列为阻碍。新增比较论文见 `Y-le-RPD-proof.zh-CN.md`：在这一已接受前提下给出 Y ≤ 当前列 RPD 的纸面证明，尚非整体 Lean 形式化。此前 IPD 候选上界研究已交付，本条不重启旧目标。

当前目标只寻找八个目标序数的具体 IPD 标准式上界，并尽量缩紧，不要求精确对应或完整证明。

采用的输入：BO 是 Buchholz ordinal，以 BMS `(0,0,0)(1,1,1)` 为参照；BMS 极限等于 Y/wY 的 `1,3`，直接作为已知结果。wY 指 ω-Y 下的 weak magma。当前 IPD 是 `../new-notation-20260914/ipd.py` 的零起始版。

前一轮重点是 RPD、Y、wY。固定保留列 ROOT 0 的二叉编码已将它们降到：

- RPD、Y：R★，计数 `1,3,10,35,125,449,1609,5725,20175,70380`。
- wY：W★，计数 `1,3,10,35,125,449,1609,5725,20175,70932`。
- 简短但更松的共同上界 H：`1,3,10,36`。

RPD 编码是 M0(q)=q()，M(k+1,q)=0(SELF(),M(k,q))；词递归 E(w)=0(M(len(w)-1,w0),E(tail))，单字词为地址叶。源数据列整体加 1，结构头 0 为额外保留空列。匹配控制必为正父，复制固定标签 0；额外父 0 控制通过保留覆盖的 T 步处理。

轮廓边界分别为 `0(0(*),.)` 与 `0(0(0(*),.),.)`。标准起点 B 是 `A2[2][2] T^5 ([2] T)^4 [3]`。W★=T^151444(B)，R★=T^151996(B)，完整树 Python 与实际 NER 回放一致，计数精确。

以 RPD-Y-wY-tightening.zh-CN.md 为三者最新结论。总报告第 2—11 节保存第一阶段编码。旧的 CAP/扁树/缓冲方案不能替代最终状态；TPD 最终第一阶段编码是二叉形状链，数字词直接作为尾部，使用第零轮廓逼近。

## 后续：ARD、TPD 已进一步缩紧

用户要求“先不证明”，本轮不重启完整证明或旧目标，只寻找更小候选。以 `ARD-TPD-tightening.zh-CN.md` 为本轮结论：

- ARD：A★=T^151811(B)，末列计数 70565。
- TPD：T★=T^151333(B)，末列计数 71043。
- 与 R★、W★ 共用上述十列分支的前九列。候选图满足 R★<A★<W★<T★<H，不能推出源序数同序。
- ARD 用一个固定标签，E_A(k,q)=0(0(k,SELF),q)，种子链包络 C_A=0(0(p,SELF),SELF)。
- TPD 用固定 0、1；N_a 是 0 一元树，S(shape)=0(N_a0,…,N_ad−1,1)，B_S=1(1)。H(shape)=0(0(SELF),S(shape))，E_T=0(H(shape),D(digits))。最终形状头只需超过位置标记，不需先超过所有完整数字词。
- TPD 理论包络 0(0(0(SELF),1(1)),.)；标准轨道的最后可用控制树为 0(0(0(SELF),2),.)，未声称它恰等于理论包络。
- `ard_tpd_anchor_probe.py` 是最终新编码；不要用早期探索的更大 TPD 形状头替代。`ard_tpd_profile_witness.py` 找轨道阈值，`anchor_ner_witness.cjs --ard-tpd` 用实际 NER 独立回放。

32 个 ARD 种子链捕获与 32 个 TPD 种子捕获已检查；最终编码测试无失败、无预算跳过。完整树见证和实际 NER 的图、计数一致。仍然是候选上界，不是完成全域覆盖秩归纳或全局最优性证明。未修改任何记号规则。

Y 极限可能等于 wY(1,4) 只作线索，未用作前提、未另行证明。Y 上界另依赖旧 Y→RPD 规范图/预降模拟。BO/BMS 的紧候选另依赖三角转换；较松备选为 A1[2]。

本轮研究交付已完成：八项均有研究级候选上界；没有声称精确对应或完成新的全局良序认证。所有工作在本研究目录；发布仓库只读，未 commit/push。测试有显式时间、步数、节点/槽限制，全部自建测试命令均已返回退出状态，没有留下后台搜索。
